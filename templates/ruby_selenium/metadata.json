{
  "name": "ruby_selenium",
  "description": "Gauge template for Ruby plugin and Selenium as driver to interact with a web browser",
  "version": "0.0.1",

  "postInstallCmd" : "ruby create_gemfile.rb && bundle install && rm create_gemfile.rb",
  "postInstallMsg" : "Run specifications with \"bundle exec gauge specs\" in project root."
}
