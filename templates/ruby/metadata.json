{
  "name": "ruby",
  "description": "Gauge template for Ruby plugin",
  "version": "0.0.2",

  "postInstallCmd" : "ruby create_gemfile.rb && bundle install && rm create_gemfile.rb",
  "postInstallMsg" : "Run specifications with \"gauge specs/\"."
}
